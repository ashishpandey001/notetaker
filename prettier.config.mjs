/** @type {import("prettier").Config} */

const config = {
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  printWidth: 180,
  arrowParens: 'always',
  bracketLine: true,
};

export default config;
